const path = require('path');

module.exports = {
	resolve: {
		alias: {
			"asab-webui-kit-lite": path.resolve('.', 'src')
		},
		// modules: [
		// 	path.resolve('..'),
		// 	'node_modules',
		// ]
	},
}

